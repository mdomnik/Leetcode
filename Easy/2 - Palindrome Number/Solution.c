#include <stdio.h>
#include <unistd.h>
#include <

int isPalindrome(int x) 
{
        if (x < 0)
            return (0);
        if ()
}

int main (void)
{
    int i = 123321;
    
    printf("%d", isPalindrome(i));
}